import Spinner from 'react-bootstrap/Spinner'

const Loading = () => {
    return (
        <div id="loading">
            <Spinner variant="success" size="lg"/>
        </div>
    )
}

export default Loading

